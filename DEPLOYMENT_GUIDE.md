# دليل نشر تطبيق Sada 249 على AWS

## 📋 نظرة عامة

تم إصلاح تطبيق Sada 249 وجعله جاهزاً للنشر على AWS. التطبيق الآن يعمل كصفحة HTML واحدة (Single Page Application) باستخدام React 19 من CDN.

## 🚀 طرق النشر على AWS

### الطريقة 1: Amazon S3 Static Website Hosting (الأبسط)

1. **إنشاء S3 Bucket:**
   - افتح AWS Console واذهب إلى S3
   - أنشئ bucket جديد (مثلاً: `sada249-archive`)
   - اختر المنطقة المناسبة (مثلاً: `us-east-1`)

2. **تفعيل Static Website Hosting:**
   - في إعدادات الـ bucket، فعّل "Static website hosting"
   - اضغط على "Edit" واختر "Enable"
   - ضع `index.html` في حقل "Index document"
   - ضع `index.html` أيضاً في حقل "Error document"
   - احفظ التغييرات

3. **رفع الملفات:**
   - ارفع ملف `index.html` إلى الـ bucket
   - اجعل الملف public (يمكنك استخدام ACL أو Bucket Policy)

4. **إعداد الأذونات:**
   - أضف Bucket Policy للسماح بالوصول العام:
   ```json
   {
     "Version": "2012-10-17",
     "Statement": [
       {
         "Sid": "PublicReadGetObject",
         "Effect": "Allow",
         "Principal": "*",
         "Action": "s3:GetObject",
         "Resource": "arn:aws:s3:::sada249-archive/*"
       }
     ]
   }
   ```

5. **الوصول للموقع:**
   - ستحصل على URL من S3 (مثلاً: `http://sada249-archive.s3-website-us-east-1.amazonaws.com`)

### الطريقة 2: AWS Amplify Hosting (الأفضل)

1. **توصيل GitHub Repository:**
   - افتح AWS Amplify Console
   - اختر "New app" > "Host web app"
   - اختر GitHub (أو CodeCommit أو Bitbucket)
   - اختر repository الخاص بـ Sada 249

2. **إعدادات البناء:**
   - Amplify سيتعرف تلقائياً على أنه تطبيق React
   - إعدادات البناء الافتراضية ستكون:
     - Build command: `npm run build`
     - Publish directory: `build`

3. **تعديل إعدادات البناء:**
   - بما أن لدينا تطبيق HTML واحد، نحتاج لتعديل:
     - Build command: `cp index.html build/`
     - Publish directory: `build`
   - أو ببساطة: اجعل root directory هي `public` واستخدم `index.html` مباشرة

4. **النشر:**
   - اضغط "Save and deploy"
   - ستحصل على دومين افتراضي من Amplify (مثلاً: `https://main.d1234567890.amplifyapp.com`)

5. **ربط دومين مخصص (اختياري):**
   - في إعدادات Amplify، اختر "Domain management"
   - أضف دومينك المخصص (مثلاً: `sada249.com`)
   - اتبع التعليمات لإعداد DNS records

### الطريقة 3: Amazon CloudFront + S3 (الأكثر أماناً)

1. **إعداد S3 Bucket:**
   - أنشئ S3 bucket (مثلاً: `sada249-archive`)
   - ارفع ملف `index.html`
   - **لا** تفعل static website hosting
   - اجعل الـ bucket private

2. **إنشاء CloudFront Distribution:**
   - افتح CloudFront Console
   - أنشئ distribution جديد
   - في "Origin domain" اختر S3 bucket الخاص بك
   - في "S3 bucket access" اختر "Yes use OAI (bucket can restrict access to only CloudFront)"
   - اترك الإعدادات الأخرى كما هي
   - أنشئ distribution

3. **إعداد Default Root Object:**
   - في إعدادات الـ distribution، ضع `index.html` كـ "Default Root Object"

4. **الوصول:**
   - ستحصل على CloudFront URL (مثلاً: `https://d1234567890.cloudfront.net`)
   - يمكنك ربط دومين مخصص لاحقاً

## 🔧 ملفات التكوين المرفقة

### ملف `index.html`
- التطبيق الكامل في ملف HTML واحد
- يستخدم React 19 من CDN
- يدعم التوجيه (Routing) باستخدام Hash URLs
- مناسب للنشر المباشر

### ملفات المصدر الأصلية
- `App.tsx`, `components/`, `contexts/` - ملفات TypeScript الأصلية
- `package.json` - تم تحديثه لدعم ES modules
- `vite.config.ts` - تم تبسيطه

## 🛡️ إعدادات الأمان

### إذا استخدمت S3 فقط:
- استخدم Bucket Policy للسماح بالوصول العام فقط للقراءة
- لا تستخدم AWS Access Keys في الكود

### إذا استخدمت CloudFront:
- استخدم Origin Access Identity (OAI) لمنع الوصول المباشر إلى S3
- يمكنك إضافة WAF لحماية إضافية

## 📊 المراقبة والتتبع

### CloudWatch
- Amplify و CloudFront يدعمان CloudWatch تلقائياً
- يمكنك مراقبة عدد الزيارات والأخطاء

### Google Analytics (اختياري)
- يمكنك إضافة Google Analytics script إلى `index.html`

## 🔄 التحديثات المستقبلية

### لتحديث التطبيق:

**Amplify:**
- ارفع التغييرات إلى GitHub
- Amplify سينشر التحديثات تلقائياً

**S3:**
- ارفع ملف `index.html` الجديد
- امسح cache إذا كنت تستخدم CloudFront

**CloudFront:**
- ارفع ملف `index.html` الجديد إلى S3
- أنشئ invalidation في CloudFront

## 🌐 إعدادات DNS (للدومين المخصص)

### إذا استخدمت Route 53:
1. أنشئ Hosted Zone لدومينك
2. أضف A record يشير إلى:
   - CloudFront distribution (لو استخدمت CloudFront)
   - أو Amplify domain

### إذا استخدمت DNS provider خارجي:
- أضف CNAME record يشير إلى:
  - CloudFront: `d1234567890.cloudfront.net`
  - أو Amplify: `main.d1234567890.amplifyapp.com`

## 📝 ملاحظات مهمة

1. **اللغة:** التطبيق يدعم اللغة العربية والإنجليزية
2. **التوجيه:** يستخدم Hash Routing (#home, #form, #archive, #admin)
3. **التصميم:** متجاوب ويعمل على الهواتف والأجهزة اللوحية
4. **البيانات:** البيانات محفوظة مؤقتاً في memory (ستختفي عند تحديث الصفحة)

## 🆘 استكشاف الأخطاء

### المشكلة: الصفحة بيضاء
- افتح Developer Tools (F12)
- تحقق من Console للأخطاء
- تأكد من تحميل React من CDN

### المشكلة: التوجيه لا يعمل
- تأكد من أن الـ URL يحتوي على # (مثلاً: `/#home`)
- في S3، تأكد من أن "Error document" = `index.html`

### المشكلة: الخطوط العربية لا تظهر
- تحقق من تحميل خط Cairo من Google Fonts
- تأكد من أن الـ CSS يحتوي على `dir="rtl"`

## 📞 الدعم

إذا واجهت مشاكل في النشر:
1. تحقق من AWS Console للأخطاء
2. راجع CloudWatch logs
3. تأكد من أن الأذونات صحيحة
4. جرب فتح الموقع في وضع التصفح الخاص (Private/Incognito)

---

**تم التحديث:** يناير 2025  
**الإصدار:** 1.0.0
