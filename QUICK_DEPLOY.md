# نشر سريع على AWS S3

## الخطوات في 5 دقائق:

### 1. إنشاء S3 Bucket

1. افتح AWS Console → S3
2. اضغط "Create bucket"
3. أدخل اسم الـ bucket (مثلاً: `sada249-archive`)
4. اختر المنطقة (مثلاً: US East N. Virginia)
5. اترك الإعدادات الأخرى كما هي
6. اضغط "Create bucket"

### 2. تفعيل Static Website Hosting

1. افتح الـ bucket الذي أنشأته
2. اذهب إلى "Properties"
3. انزل لأسفل إلى "Static website hosting"
4. اضغط "Edit"
5. اختر "Enable"
6. في "Index document" اكتب: `index.html`
7. في "Error document" اكتب: `index.html`
8. اضغط "Save changes"

### 3. رفع الملف

1. في الـ bucket، اذهب إلى "Objects"
2. اضغط "Upload"
3. اسحب ملف `index.html` أو اختره
4. اضغط "Upload"

### 4. إعداد الأذونات

1. في الـ bucket، اذهب إلى "Permissions"
2. انزل لـ "Bucket Policy"
3. اضغط "Edit"
4. أضف هذا السياسة (استبدل `sada249-archive` باسم bucket الخاص بك):

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::sada249-archive/*"
    }
  ]
}
```

5. اضغط "Save changes"

### 5. الحصول على الرابط

1. في الـ bucket، اذهب إلى "Properties"
2. انزل لـ "Static website hosting"
3. ستجد "Bucket website endpoint"
4. هذا هو رابط موقعك! (مثلاً: `http://sada249-archive.s3-website-us-east-1.amazonaws.com`)

## 🎉 تم! موقعك الآن يعمل!

افتح الرابط في المتصفح وسيعمل التطبيق بشكل كامل.

## 📌 ملاحظات مهمة:

- الرابط سيبدأ بـ `http://` وليس `https://` (هذا طبيعي لـ S3 static hosting)
- إذا أردت HTTPS، استخدم CloudFront أو Amplify
- البيانات محفوظة مؤقتاً في المتصفح (ستختفي عند تحديث الصفحة)
- للحصول على قاعدة بيانات حقيقية، استخدم DynamoDB أو RDS

## 🔧 استكشاف الأخطاء:

إذا لم يعمل الموقع:

1. **صفحة بيضاء؟**
   - افتح Developer Tools (F12)
   - تحقق من Console للأخطاء
   - تأكد من تحميل ملف index.html كاملاً

2. **404 Error؟**
   - تأكد من أن اسم الملف هو `index.html` بالضبط
   - تأكد من أن الأذونات (Bucket Policy) مضبوطة بشكل صحيح

3. **CSS أو JavaScript لا يعمل؟**
   - تأكد من أن Content-Type للملف هو `text/html`
   - جرب فتح الموقع في وضع التصفح الخاص (Incognito)

## 🚀 الخطوة التالية:

بعد أن يعمل الموقع، يمكنك:
1. ربط دومين مخصص (Domain)
2. إضافة HTTPS باستخدام CloudFront
3. إضافة قاعدة بيانات DynamoDB
4. إضافة نظام تسجيل دخول للمشرفين

## 📞 للمساعدة:

راجع ملف `DEPLOYMENT_GUIDE.md` الكامل للحصول على تفاصيل أكثر!
